debug: true
spring:
  freemarker:
    charset: UTF-8
    cache: false
    check-template-location: true
    content-type: text/html
    template-loader-path: classpath:/templates/
    suffix: .ftl
    enabled: true
  output:
    ansi:
      enabled: detect

server:
  compression:
    enabled: true
  port: 8080
  tomcat:
    accesslog:
      enabled: true
